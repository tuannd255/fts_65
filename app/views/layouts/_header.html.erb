<header class="navbar navbar-fixed-top navbar-inverse">
  <div class="container">
    <%= link_to t(:title), root_path, id: "logo" %>
    <nav>
      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
          <% if current_user.is_admin? %>
            <li><%= link_to t("home.name"), admin_root_url %></li>
            <li><%= link_to t("subjects.title"), admin_subjects_path %></li>
            <li><%= link_to t("users.title"), admin_users_path %></li>
            <li><%= link_to t("questions.title"), admin_questions_path %></li>
            <li><%= link_to t("exams.title"), admin_exams_path %></li>
          <% else %>
            <li><%= link_to t("home.name"), root_url %></li>
            <li><%= link_to t("exams.title"), exams_path %></li>
          <% end %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
              <%= current_user.name %><b class="caret"></b>
            </a>
            <ul class="dropdown-menu">
              <li>
                <%= link_to t("users.setting"),
                  edit_user_registration_path(current_user) %>
              </li>
              <li>
                <%= link_to t(:log_out), destroy_user_session_path,
                  method: :delete %>
              </li>
            </ul>
        <% else %>
          <li>
            <%= link_to t(:log_in), new_user_session_path %>
          </li>
          <li>
            <%= link_to t("home.log_in_with_fb"),
              user_facebook_omniauth_authorize_path %>
          </li>
        <% end %>
      </ul>
    </nav>
  </div>
</header>
